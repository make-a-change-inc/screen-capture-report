# 管理画面 元仕様・実装対応表

更新日: 2026-08-05

## 判定基準

- **実装済み**: 実データ、権限、監査、自動テストを含めてCloudflare本番へ反映済み。
- **接続待ち**: コードは未設定を正しく検出するが、外部サービス側の契約・権限・資格情報が必要。
- **境界外**: 現在の承認済みデータ契約に反し、データを捏造・過剰収集するため実装しない。

## 実装済み

| 元仕様領域 | 実装内容 |
|---|---|
| 経営ダッシュボード | 実測時間、削減見込み、対象人数、同期レポート、週・部署絞り込み、前週比較 |
| 業務分析・AI化候補 | カテゴリ／部署集計、根拠表示、候補状態・担当・次アクション・判断理由 |
| レポート | 確定週報一覧・検索・暗号化本文・版履歴・レビュー／確定／配信状態・PDF印刷 |
| 従業員 | 読み取り専用一覧、部署、端末数、最終同期、最新レポート |
| 端末・収集状況 | 匿名日次heartbeat、取得率、欠損、停止理由、アプリ版、ポリシー版 |
| 収集ポリシー | 利用目的、全体停止、除外アプリ・URL・時間帯、版管理、Windows端末適用 |
| 分類・品質 | 日本語表示名、試算係数、未分類品質、訂正履歴、決定的な再集計版 |
| プライバシー | 同意履歴、データ申請、対象指定削除、リーガルホールド、削除証跡 |
| テナントデータ | Owner限定JSON出力、秘密情報除外、申請・確認・証跡付き全業務データ削除 |
| ユーザー・権限 | 複数管理者、Owner／Manager／Auditor、最後のOwner保護、ユーザー停止 |
| 認証 | HttpOnly Cookie、CSRF、レート制限、TOTP MFA、PBKDF2、パスワード変更、セッション失効 |
| 監査・保持 | ログイン、閲覧、出力、設定、分類、削除、配信等の監査、期限削除、ホールド除外 |
| 配信 | メール／Slack／Teams設定、暗号化宛先、手動配信、時間単位スケジュール、成功・失敗履歴 |

## 外部サービス接続待ち

| 項目 | 現在の状態 | 完了に必要なもの |
|---|---|---|
| Cloudflare Email Service | Workerは `EMAIL` bindingと`REPORT_EMAIL_FROM`がある場合だけ送信する。未設定時は503と失敗履歴。Wrangler 4.115.0と4.118.0の双方で、OAuth権限一覧には `email_sending (write)` がある一方、Email Sending照会はCloudflare API `Unauthorized (2036)`。 | Cloudflareアカウント側のEmail Sending利用可否確認、送信ドメインのonboard、`send_email` binding、送信元アドレス、実受信先 |
| SSO／IdP連携 | 汎用OIDC Authorization Code + PKCE、state/nonce、Discovery、JWKS署名検証、許可ドメイン、JITユーザー作成、role連携を実装。未設定企業は未接続と明示。 | 採用IdPのissuer、client ID／secret、callback URL登録、許可メールドメイン、初回role |

外部設定がない状態で「配信済み」「SSO有効」と表示する実装は行わない。

## 現在のプライバシー境界により対象外

| 元仕様候補 | 判断 |
|---|---|
| 生スクリーンショット閲覧 | 管理サーバーへ送信しない。端末内限定を維持する。 |
| ウィンドウタイトル、URL、詳細作業ログ | 管理サーバーへ送信しない。運用件数だけを同期する。 |
| 本人向け日報・個人ランキング | 現行API契約に存在せず、本人監視へ逸脱するため管理画面に作らない。 |
| 月次・効果測定レポート本文 | Windowsアプリが生成・確定・同期する契約が未定義。週次管理レポートから算出できる集計は画面に表示するが、存在しない本文は捏造しない。 |

## リリース

- PR #13: 基盤・本物の管理画面・セッション認証
- PR #14: レポート運用・AI候補・分類・プライバシー
- PR #16 / #17: Windows heartbeat・収集ポリシー・CI修正
- PR #18: 分類訂正・リーガルホールド・対象指定削除
- PR #19: 複数管理者・RBAC・MFA・セッション管理
- PR #20: PDF・メール／Slack／Teams配信基盤
- PR #21: テナント出力・削除・PBKDF2・パスワード変更
- PR #23: 企業別OIDC SSO
- PR #24: 廃止済みブラウザパスワード認証コードの削除

最新の本番反映:

- main: `1f50650fd4eb9d8fcedda211e909ba0c4a3e4d83`
- GitHub Actions: `30960913489` 成功
- Cloudflare deployment version: `83d09101-c8f8-49ae-8e3a-a79636567d99`

Cloudflare本番URL: `https://screen-capture-report-admin.m-okamura-8e7.workers.dev/`
